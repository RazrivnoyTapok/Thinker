<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Thinker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/darkest_gray</item>
        <item name="colorPrimary">@color/dark_gray</item>

        <item name="colorOnPrimary">@color/white_text</item>
        <item name="android:textColor">@color/white_text</item>
        <item name="android:textAppearanceButton">@color/white_text</item>

        <item name="android:popupMenuStyle">@color/dark_gray</item>
    </style>
</resources>